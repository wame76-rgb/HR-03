# PROTOCOL DE COMPORTEMENT: SESSION_HANDOVER_MASTER (v16)
## وثيقة التسليم والاستلام الهندسية الكبرى - نظام إدارة الموارد البشرية (HR System)

> **تنبيه للوكيل القادم (Tech Lead / Monkey Code / Google Studio):** 
> هذا الملف هو الذاكرة التشغيلية الوحيدة والمعتمدة للمشروع. لقد تم تصميمه برؤية معمارية صارمة ليكون بمثابة "جسر تعاقب رقمي" (Session Transfer State) يربط بين جلسات العمل المتعاقبة. عند انطلاق أي جلسة جديدة، أنت ملزم برمجياً بقراءة هذا الملف فوراً، ومطابقة محتوياته مع الملفات المرفوعة في سياق العمل لتحديد نقطة البدء دون تشتيت أو هدر للتوكنز.

---

### أولاً: خريطة وقائمة المصادر المطلوبة في الـ Sources (Sources Verification Checklist)
يُمنع منعاً باتاً البدء في العمل إذا نقص أي ملف من الملفات التالية في لوحة الـ (Sources). في حال غياب أحدها، توقف فوراً واطلب من المستخدم رفعه:

| اسم الملف / المصدر | نوعه | الأهمية الفنية والبرمجية | الحالة في الجلسة الحالية |
| :--- | :--- | :--- | :--- |
| `PROJECT_CONSTITUTION.md` | Markdown | الدستور البرمجي الحاكم وقواعد البيئة والوكلاء والقيود الصارمة [98، 101]. | **موجود ونشط** |
| `PROJECT_ANALYSIS-2.md` | Markdown | الهيكل التفصيلي لبيانات الجداول والعلاقات في قواعد البيانات (2025 و TimeSheet) [110، 118]. | **موجود ونشط** |
| `02-general-project-instructions.md` | Markdown | التعليمات العامة لطبقة البيانات والواجهة ونظام التصميم الموحد [1، 3]. | **موجود ونشط** |
| `03-input-screen-task-instructions.md`| Markdown | المواصفات الصارمة وسياسات التركيز والتنقل لشاشة المتغيرات [8، 17]. | **موجود ونشط** |
| `CODEBASE_AND_TREE.txt` | Text | شجرة مجلدات المشروع بالكامل وسجل الأكواد البرمجية النشطة لتفادي تشتت المسارات [28]. | **موجود ونشط** |
| `1.bmp` إلى `7.bmp` | Images | اللقطات الحية الملتقطة للشاشات في مراحل تشغيلها المختلفة لمطابقتها بصرياً [22، 27]. | **موجود ونشط** |

---

### ثانياً: أرشيف المهام المنجزة بالكامل (Completed Milestones Archival Log)
سجل هندسي تراكمي غير قابل للحذف يوثق كل محطة تم إغلاقها واختبارها محلياً بنجاح (Prevent Regression):

*   **المحطة الأولى (تأمين الاتصال وقفل السجلات):**
    *   **الإنجاز:** بناء وتثبيت طبقة البيانات `db_connection.py` وتفعيل بروتوكول `execute_with_retry` لحماية البيانات من التجمد وأقفال Access [4، 103].
*   **المحطة الثانية (هيكلة واجهة المتغيرات وتصميم الـ 3 أعمدة):**
    *   **الإنجاز:** إعادة تخطيط واجهة `variables_window.py` بالكامل لتتوزع RTL على ثلاثة أعمدة مرنة متجاوبة مع الشاشات الصغيرة وبدون استخدام QSplitter [10، 39].
*   **المحطة الثالثة (التركيز وسياسة التنقل الذكي - Focus & Navigation Policy):**
    *   **الإنجاز:** ضبط محاذاة التركيز التلقائي على الرقم المالي عند الفتح، والتعامل الذكي مع مفاتيح Enter والأسهم للتحرك السلس بين الحقول [18، 20].
*   **المحطة الرابعة (إصلاح العيوب الـ 11 وتكامل الشاشة):**
    *   **الإنجاز:** إصلاح تباين الكومبوبوكس وظهور الأسهم، وعرض الاختصارات الإنجليزية للمتغيرات (X, R, WH) في الجداول [3، 143]، وتحسين أداء التقارير اللحظية بالذاكرة عبر persistent ODBC [4].

---

### ثالثاً: المحطة النشطة الحالية (Station 5 - Current Active Sprint & Pending Tasks)
*   **المهمة الحالية:** شاشة تهيئة بداية الشهر والعمليات التأسيسية المتقدمة (`hr_system/ui/month_prep_window.py`) [125].
*   **العيوب واللمسات الـ 14 التي تم حلها وصقلها في المظهر v15:**
    1.  **بيان المستخدم:** دمج الاسم والرقم المالي في سطر واحد بفاصل `|` مع اختصار الاسم ليصبح ثلاثياً فقط للتناسق البصري: `"المستخدم الحالي: وليد ممدوح يوسف  |  الرقم المالي: 2107"` [54].
    2.  **كارت الاتصال بقاعدة البيانات:** إلغاء عملية الفحص اليدوي الوهمي للمسارات، والاستعلام الفوري عن حالة اتصال الكائن الممرر للشاشة `self.db` لعرض شارة خضراء تفصيلية بالأسفل توضح تفاصيل الملف المادي النشط `TIME SHEET.mdb` والجدول والمسار الحقيقي طمأنينة للمستخدم [125].
    3.  **تصغير حقل الشهر:** محاذاة أزرار `[-]` و `[+]` لتصبح شبه ملتصقة بمربع الشهر (QLineEdit) لمحاكاة دقة شاشة المتغيرات [11].
    4.  **تثبيت أعمدة التقويم:** دمج عناوين أيام الأسبوع في جدول `QGridLayout` واحد ثابت RTL يضمن بقاء عمود الجمعة هو الأول على اليمين دوماً لمنع تذبذب الأعمدة عند تغيير الأشهر [39].
    5.  **تطوير كروت الأيام التفاعلية:** الحفاظ التام على محتوى الكارت (رقم اليوم في المنتصف، وتحته ملصق الحالة الصغير) وتغيير ألوان الخلفيات والنصوص فقط دون تشويه الإطار في الحالات الأربعة:
        *   *الجمعة والسبت (الراحة R):* خلفية رمادية باهتة `#f1f3f4` وتسمية "راحة (R)" [120].
        *   *الأحد (العمل عن بعد WH):* خلفية لافندر ناعمة `#f3e5f5` ونصوص بنفسجية `#8e24aa` وتسمية "عمل عن بعد (WH)" [13].
        *   *أيام العمل الطبيعية:* خلفية بيضاء ناصعة ومحاطة بإطار خفيف `#dadce0` وتسمية "عمل".
        *   *العطلات الرسمية (H):* خلفية زرقاء بارزة `#1a73e8` وخط أبيض وتسمية "عطلة (H)" لدعم الإجازات الطويلة المتصلة.
    6.  **تطوير الـ Checkboxes والتحقق الوقائي:**
        *   مفتاح `[ ] تنشيط تحديد العطلات الرسمية (H)`: افتراضياً غير مفعل، وتكون الخلايا مقفلة لتجنب الخطأ. عند تفعيله، يفتح التقويم بالكامل يدوياً.
        *   مفتاح `[ ] تفعيل العمل عن بعد (الأحد - WH)`: مفعل تلقائياً عند فتح الشاشة [13].
        *   *رسالة التحقق الذكي:* عند النقر على `[بدء تهيئة الشهر]`، يفحص الكود عدد الأيام المعلمة كـ `H` ويظهر تأكيداً RTL ملائماً للمستخدم يمنع السهو البشري قبل إطلاق خيط المعالجة `QThread` بالخلفية [4، 104].
    7.  **المظهر النهائي والأزرار:** إرجاع أزرار الإجراءات `[بدء تهيئة الشهر]` و `[رجوع]` للعمود الأيمن لإعطاء شبكة التقويم حرية كاملة للتمدد الرأسي، وحذف مقبض الحجم الصغير (Size Grip) كلياً للحفاظ على تصميم Pixel المسطح.

---

### رابعاً: المهام المفتوحة والمستقبلية (Future Roadmap & Open Backlog)
1.  **المحطة السادسة (البحث المتقدم برقم العامل):** بناء نافذة البحث السريع وتصفية السجلات وتجهيز التقرير التفصيلي.
2.  **المحطة السابعة (نظام التقارير الشامل والطباعة):** توليد مستندات PDF المنسقة وصياغة تقارير الحضور والانصراف السنوية [42].
3.  **المحطة الثامنة (إغلاق وتأمين النظام):** المراجعة النهائية لجودة الأكواد (Code Review) بمشاركة Claude وحل مشكلات كلمات المرور المشفرة لملفات الـ mdb بالكامل [56].

---

### خامساً: القوانين التقنية المقدسة (Non-Negotiable Technical Directives)
*   **بروتوكول الـ Transactions في Access:** كل عمليات الإدراج المجمعة (Bulk insert في جدول `var_op` ذو الـ 1.8 مليون سجل) يجب أن تنفذ بالكامل داخل معاملة واحدة متماسكة وتحت مظلة `execute_with_retry` لحماية قاعدة البيانات [4، 59].
*   **عزل خيوط المعالجة:** يمنع منعاً باتاً تنفيذ أي استعلام تاريخي طويل على الخيط الرئيسي للبرنامج، واستخدام الـ `QThread` دائماً للحفاظ على استجابة الشاشة [4، 104].
*   **نمط المسارات الديناميكية:** الاعتماد التام على `BASE_DIR` لحساب مسارات الملفات مثل `config.ini` و `key.key` جغرافياً لتجنب مشكلات بيئة التشغيل على جهاز المستخدم [3، 55].
*   **الحفاظ على الأكواد المستقرة:** يُحظر تماماً حذف أو تعديل أي ميزة أو دالة قديمة عاملة؛ التطوير يكون بالإضافة المتوافقة والإكمال الجراحي فقط [110].
