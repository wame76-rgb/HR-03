# 📂 SESSION_HANDOVER_MASTER — سجل الحالة الحية والتعاقب المطور (v19)
> **بروتوكول سيادي دائم**: هذا الملف هو مصدر الحقيقة الفوري والوحيد للتسليم والتسلم بين الجلسات. يقرأ بالكامل عند الفتح ويُحدث بالكامل عند الإغلاق. يُحظر تماماً تعديل أو حذف أي دالة قديمة مستقرة أو ميزة عاملة (Prevent Regression).

---

## 🚀 [PART 1: HANDOVER_CURRENT] — الموقف النشط والجلسة الحالية
- **الحالة:** تم إنجاز جلسة HR-03 بالكامل: إعادة تصميم شاشة تهيئة بداية الشهر برمجياً وبصرياً، وحل انهيار التهيئة الثاني (HY000 + القيد الرئيسي EmpId+EntryDate).
- **الإنجاز (v2.0.0 - month_prep_window.py):**
  1. **إصلاح كراش قاعدة البيانات:** فحص مسبق صامت (pre_check_month) يحدد إن كان الشهر مُهيأ مسبقاً، ثم رسالة تأكيد "هذا الشهر تمت تهيئته مسبقاً، هل تريد إعادة التهيئة ومسح الحركات الحالية؟"، يليها حذف آمن وإدراج مجزأ بدفعات 200 صف مع commit بعد كل دفعة عبر execute_with_retry (لا فتحات pyodbc خام).
  2. **التحويل الانتقائي:** فقط الحالات الثلاث (H / R / WH) تُعبأ برموزها، وأيام العمل العادية تبقى بقيمتها الافتراضية (0) ليدخل المستخدم الحركات لاحقاً.
  3. **حارس الجمعة/السبت/الأحد:** جلب أي قيمة سابقة غير صفرية قبل الحذف وحفظها كما هي دون الكتابة فوقها (حماية التعديلات اليدوية).
  4. **تخطيط RTL:** شبكة التقويم بالكامل على اليمين تبدأ من الأحد (أقصى اليمين) حتى السبت (أقصى اليسار)، وفوقها صندوق الشهر بصيغة MM-YYYY مع زرّي (+) و(-) ملاصقين وإحصائيات الشهر في المساحة المتبقية. يساراً: سجل الإخراج + حالة الاتصال + زرا التهيئة/الرجوع.
  5. **الشكل الجمالي:** شبكة Excel متصلة بخطوط رفيعة #D1D5DB، خانتا اختيار بعلامة (✓) واضحة داخل الصندوق دون تظليل (WH بنفسجي #8E24AA / H أزرق #0369A1)، ألوان الخلايا حسب المواصفات (جمعة/سبت وردي #FFEAEA، أحد بنفسجي #F3E5F5، عطلات أزرق #E0F2FE، عمل أبيض).
  6. **خيطان مستقلان:** MonthPreCheckThread للفحص المسبق وMonthPrepThread للتهيئة، كلاهما عبر كائن DatabaseConnection معزول.
* **تاريخ الجلسة الحالية:** 2026-08-24
* **المرحلة والهدف النشط:** إغلاق محطة HR-03 والجاهزية للمحطة السابعة (شاشة البحث العام المتقدم).
* **الموقف البرمجي اللحظي (v2.0.0 - Production Ready & Hardened):**
  - **تطابق البناء (TypeError Fix):** توقيع `MonthPrepWindow(config_manager, db_connection, user_data, parent=None)` متطابق تماماً مع الاستدعاء في `main_window.py` السطر 287 (`self.config, self.db, self.user_data, self`) — لا انهيار TypeError.
  - **طبقة db_connection.py:** إضافة `pre_check_month` و `get_month_present_days` و `delete_month_records`، وإعادة بناء `initialize_month_records` بالمعاملات الجديدة (code_map، batch_size=200، preserve_weekend_overrides، interrupt_check). وإضافة مهلة اتصال قابلة للضبط `connect_timeout=8`، وجعل `InterruptedError` (إلغاء المستخدم) يُعاد طرحه فوراً دون إعادة محاولة.
  - **التحقق من اكتمال الأيام:** أي يوم من الشهر غير موجود في var_op يُوقف العملية برسالة خطأ واضحة قبل أي كتابة.
* **العوائق الحالية:** لا عوائق فنية. ملاحظة بيئة: الوكيل بلا سائق Access ولا PySide6 — الاختبار الوظيفي النهائي يتم على جهاز المستخدم (Windows, `C:\PROJECTS\amr\external_control_MONKEY_CODE_V3\hr_system\`).


---

## 🎯 [NEXT_MILESTONES] — المهمات المفتوحة والباكلوج المستقبلي
*تُنفذ المهام بترتيب صارم متوافق مع الدستور التشغيلي:*
1. **جلسة HR-03 (DONE ✅):** إعادة تصميم شاشة تهيئة بداية الشهر وحل انهيار ODBC HY000 والقيد الرئيسي، والتحويل الانتقائي، وحارس الجمعة/السبت/الأحد، والتقويم الجداري على نمط Excel RTL.
2. **المحطة السابعة (Station 7 - Pending):** بناء وتصميم شاشة البحث العام المتقدم برقم الموظف أو الاسم.
3. **المحطة الثامنة (Station 8):** هندسة وبناء التقارير الإحصائية والطباعة الورقية وتوليد مستندات PDF.

---

## 📜 [PART 2: HANDOVER_ARCHIVE] — الأرشيف التاريخي التراكمي للمهام المغلقة
سجل هندسي تراكمي غير قابل للحذف يوثق كل محطة تم إغلاقها واختبارها محلياً بنجاح (Prevent Regression):

### 📅 [جلسة 18/08/2026] - v1.0.0 (توحيد البنية وتصحيحات الأمان وشاشة الدخول)
* **توحيد ملفات الإعدادات:**
  - تم حل مشكلة تكرار وازدواجية ملفات الإعدادات (`config.ini` و `key.key`) الناتجة عن المسارات النسبية [59].
  - تم نقل وتثبيت كلمات السر والمفتاح داخل مجلد التطبيق الموحد `hr_system/` [59].
  - تم تحسين دالة `BASE_DIR` في `config_manager.py` لتقرأ ديناميكياً مسار الملف الجاري تشغيله لحماية استيراد الموديلات من الخطأ البرمجي `sys.path` [34].
  - حذف النسخ المكررة خارج مجلد المشروع لتجنب اللبس والتشتت [59].
* **تحديث شاشة الإعدادات (`settings_window.py`):**
  - تم إخفاء مربعات الاختيار الخاصة بإظهار كلمات المرور في شاشات الإعدادات لزيادة مستويات الحماية [57].
  - تعديل زر الإغلاق ليصبح زر "خروج" بتصميم مريح وعريض وألوان متناسقة مع لغة تصميم التطبيق Pixel Style [57].
* **إصلاحات شاشة الدخول (`login_window.py`):**
  - تم إزالة الخلفيات الرمادية عن مربعات النصوص لتوحيد الطابع البصري [58].
  - تم حل مشكلة بطء تحميل أسماء العاملين وتجمد الواجهة عند إدخال الرقم المالي [58].
  - الحل اعتمد على إنشاء خيط منفصل (`NameFetcherThread`) كفئة فرعية من `QThread` مع دالة المؤقت `QTimer` بـ (350ms debounce) لتقليل عمليات الاستعلام المتتالية على قاعدة البيانات والتحقق من تطابق النص الأخير قبل عرض الاسم [58].
* **تحديث الشاشة الرئيسية (`main_window.py`):**
  - تخصيص رسالة الترحيب بشريط العنوان لتصبح: "مرحبا، {name} | الرقم: {id}" مع إزالة الكلمة العائمة السابقة [58].
  - إصلاح عرض الرقم المالي كعدد صحيح خالي من الكسور (تحويل الرقم من `2107.0` إلى `2107` باستخدام دالة مخصصة `_format_id()`) [58].
  - استبدال علامة الاستفهام الخاصة بزر الإعدادات بأيقونة الترس الحقيقية المضمنة في نظام Qt [58].

### 📅 [جلسة 19/08/2026] - v1.0.1 (إصلاح تداخل المكتبات ورسائل التنبيه)
* **إصلاح استيراد QCompleter:**
  - تم تصحيح استيراد `QCompleter` في `variables_window.py` ليكون من `QtWidgets` بدلاً من `QtCore` [152].
* **تنبيه الإذن الفارغ:**
  - تم إضافة رسالة تنبيه صريحة وتحذيرية للمستخدم عند الضغط على زر ترحيل إذن بقيمة فارغة لإعلامه بتفريغ الأذونات للنطاق كاملاً لتجنب الأخطاء الصامتة [152].

### 📅 [جلسة 21/08/2026] - v1.0.2 (تثبيت طبقة قاعدة البيانات db_connection.py)
* **معالجة أقواس LEFT JOIN المتداخلة:**
  - تم بناء وتصحيح استعلام جلب بيانات العامل الموحد بصيغة الأقواس المتداخلة الخاصة بـ MS Access لمنع انهيار البرنامج عند تنفيذ الاستعلام [152].
* **حماية دالة الاستعلامات وتأمين الإرجاع:**
  - إرجاع قيمة `None` آمنة للرقم المالي غير الموجود، وتأمين دالة `movement_exists` من استثناءات `IndexError` [152].
* **منع حفظ الأشباح (Ghost Saves):**
  - تم تضمين استدعاء `conn.commit()` صراحة بداخل دالة `execute_with_retry` لحماية كافة الإدراجات والتحديثات من الفقدان [152].

### 📅 [جلسة 21/08/2026] - v1.0.3 (إصلاح منطق البرمجيات والتواريخ بشاشة المتغيرات)
* **حسابات التواريخ والتقارير:**
  - إصلاح حسابات تقارير التحركات السنوية `_load_report` عبر دمج دالتي `_add_months` و `_months_between` لمنع انهيار التواريخ لسنوات مستقبلية تالفة [152].
* **إعادة بناء دالة إلغاء الإذن `_cancel_permission`:**
  - حل مشكلة الشرط الميت والتحقق الفعلي من وقوع التاريخ ضمن الشهر المالي النشط وعرض تحذير للمستخدم [152].
* **تحديث عبور السنة:**
  - تفعيل التحديث التلقائي الفوري لبيانات التقرير السنوي عند عبور حدود السنة بين ديسمبر ويناير [152].

### 📅 [جلسة 22/08/2026] - v1.0.4 (إعادة الهيكلة البصرية الأولى والـ 3 أعمدة)
* **تصميم الـ 3 أعمدة RTL:**
  - تم إعادة تخطيط واجهة `variables_window.py` لتتوزع RTL على ثلاثة أعمدة مرنة متجاوبة مع الشاشات الصغيرة وبدون استخدام QSplitter [157].
* **حل مشكلة الـ 31 يوماً دون تمرير:**
  - تثبيت خاصية `vheader.setMinimumSectionSize(10)` لضمان عرض 31 صفاً كاملاً للتحركات دون الحاجة لاستخدام أشرطة التمرير [157].
* **كروت بيان العامل الأنيقة:**
  - تصميم كروت 2x2 خالية تماماً من التداخل والحدود المزدوجة ومريحة لعين المستخدم [157].

### 📅 [جلسة 22/08/2026] - v1.0.5 (سياسة التركيز والتنقل الذكي وتلقائية التواريخ)
* **تلقائية التركيز الفتحي:**
  - نقل مؤشر الكتابة تلقائياً إلى حقل الرقم المالي فور فتح الشاشة لتوفير نقرات الماوس [157].
* **التنقل الذكي عبر لوحة المفاتيح:**
  - برمجة فلتر أحداث يتيح للمستخدم الانتقال بين الحقول باستخدام أزرار Tab والأسهم ورموز Enter للوحة المفاتيح العادية والآلة الحاسبة بنعومة تامة [157].
* **وراثة التواريخ التلقائية:**
  - ربط حقل تاريخ إلى ليرث ديناميكياً قيمة تاريخ من تلقائياً مع بقائه مرناً وقابلاً للتعديل الفردي [157].

### 📅 [جلسة 23/08/2026] - v1.1.0 (إغلاق وتكامل شاشة تهيئة الشهر وباك اند التجميع المليوني)
* **إصلاح ثغرة تجاوز تسجيل الدخول (The Boot Sequence Fix):**
  - تعديل ملف `main.py` ليجعل شاشة تسجيل الدخول `LoginWindow` هي الشاشة الأولى والوحيدة التي تُفتح عند تشغيل البرنامج تحت أي ظرف [1.1.0].
  - منع الوصول لشاشة الإعدادات قبل المصادقة، وإظهار رسائل مخصصة حسب صلاحية المستخدم (is_developer) لمنع كشف التفاصيل الفنية لغير المسجلين [1.1.0].
* **إعادة تصميم واجهة تهيئة بداية الشهر بالكامل:**
  - تحويل الواجهة لشبكة تقويم RTL فخمة وواسعة، وتثبيت عمود الجمعة أول عمود على اليمين [120، 124].
  - تفعيل الألوان التفاعلية الأربعة لحالات خلايا التقويم (الأزرق الفخم للعطلات، اللافندر للعمل عن بعد، الأبيض للعمل العادي، والرمادي المطفأ للراحة الأسبوعية) [120].
* **إصلاح كارت الاتصال بالباك اند:**
  - إلغاء القراءات الوهمية وقراءة حالة الاتصال المباشر لكائن قاعدة البيانات الفعلي الممرر للشاشة، وعرض مسار الملف المادي النشط `TIME SHEET.mdb` طمأنينة للمستخدم.

### 📅 [جلسة 24/08/2026] - v2.0.0 (HR-03: إعادة تصميم شاشة التهيئة وحل انهيار ODBC)
* **إصلاح انهيار التهيئة الثاني (HY000 + القيد الرئيسي):**
  - إضافة فحص مسبق صامت `pre_check_month` (count + أيام الشهر الموجودة) قبل أي كتابة على var_op.
  - رسالة تأكيد إعادة التهيئة: "هذا الشهر تمت تهيئته مسبقاً، هل تريد إعادة التهيئة ومسح الحركات الحالية؟".
  - حذف آمن عبر `delete_month_records` ثم إدراج مجزأ بدفعات 200 صف مع commit بعد كل دفعة، ومعالجة `InterruptedError` (إلغاء المستخدم) بطرح فوري دون إعادة محاولة.
* **التحويل الانتقائي وحارس الأمان:**
  - فقط الحالات H / R / WH تُعبأ، وأيام العمل العادية تُترك بقيمتها الافتراضية (0).
  - حارس الجمعة/السبت/الأحد: جلب القيم غير الصفرية السابقة وحفظها كما هي دون الكتابة فوقها.
* **إعادة التصميم البصري (Excel RTL):**
  - التقويم يميناً يبدأ من الأحد أقصى اليمين حتى السبت أقصى اليسار، بشبكة متصلة بخطوط رفيعة #D1D5DB.
  - صندوق الشهر بصيغة MM-YYYY مع زرّي (+) و(-) ملاصقين وإحصائيات الشهر في المساحة المجاورة.
  - خانتا اختيار بعلامة (✓) واضحة داخل الصندوق دون تظليل الخلفية (WH بنفسجي / H أزرق).
  - يساراً: سجل الإخراج + حالة الاتصال + زرا التهيئة (أخضر #059669) والرجوع (أحمر #DC2626).
* **تطابق البناء (TypeError Fix):** توقيع `MonthPrepWindow` متطابق مع الاستدعاء في `main_window.py` السطر 287.

