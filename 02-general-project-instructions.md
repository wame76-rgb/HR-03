### تعليمات عامة ثابتة — مشروع نظام الموارد البشرية (HR System)
**الغرض من هذا الملف**: سياق دائم يُعطى للوكيل الذكي في **بداية أي مهمة** على هذا المشروع، بصرف النظر عن المهمة المحددة. لا يخص شاشة واحدة، بل قواعد المشروع كله. اقرأه قبل أي تعديل كود، والتزم به دون استثناء ما لم يطلب المستخدم غير ذلك صراحة.

---

#### 1. من هو المستخدم وكيف يعمل
* اسمه **صديق**، مطور HR برمجيات، يفهم الكود جيدًا ويحب الكود النظيف المرتّب.
* **لا يحب "نص نص"** — يفضّل حلاً واضحًا مكتملاً على حل منتصف الطريق.
* **لا يوجد Git بين بيئة الوكيل وجهاز المستخدم.** التنسيق يتم عبر تبادل ملفات/أرشيفات مضغوطة. لذلك: لا تفترض أن الوكيل "يرى" آخر نسخة تلقائيًا — اعتمد فقط على ما هو مرفوع فعليًا في مساحة العمل الحالية.
* المستخدم يعمل من جهاز **Windows**، والمسار المعتاد للمشروع: `C:\PROJECTS\amr\external_control_MONKEY_CODE_V3\hr_system\`.

---

#### 2. القاعدة الذهبية: لا تنفيذ قبل التأكيد
**لا تكتب أو تعدّل أي كود فعلي قبل أن يقول المستخدم كلمة صريحة مثل "اشتغل" أو "ابدأ التنفيذ".** ناقش المواصفات أولًا، اطرح أسئلة إن وُجد غموض، اعرض خطة أو تصميمًا مقترحًا، وانتظر التأكيد. هذه القاعدة وردت صراحة في وثائق المشروع وتم الالتزام بها في الجلسات السابقة — استمر عليها.

---

#### 3. المكدس التقني — ثابت، لا تُغيّره بدون إذن
| الطبقة | التقنية |
| ------ | ------ |
| الواجهة | PySide6 (Qt for Python) |
| قاعدة البيانات | Microsoft Access (.mdb) عبر pyodbc |
| التشفير | cryptography.Fernet لكلمات مرور قاعدة البيانات |
| اللغة | Python 3 (لا يوجد python، فقط python3 في بيئة الاختبار) |

لا تقترح استبدال أي من هذه التقنيات (مثل التحويل لـ SQLAlchemy أو SQLite أو PyQt بدل PySide6) إلا إذا طلب المستخدم ذلك صراحة.

---

#### 4. نظام التصميم الموحّد (Design System) — استخرج من الكود الفعلي، التزم به حرفيًا
كل شاشة جديدة **يجب** أن تستخدم نفس القيم البرمجية لضمان الاتساق البصري وفق فلسفة تصميم Google Pixel:
* **الألوان المقدسة**:
  * اللون الأزرق المعتمد: `#1a73e8` (Google Pixel Blue)
  * لون الخلفية الرمادي الفاتح: `#f1f3f4`
  * لون الإطارات: `#dadce0`
  * لون النصوص الداكنة: `#202124`
  * لون اللافندر/البنفسجي (للعمل عن بعد WH): خلفية `#f3e5f5` ونصوص `#8e24aa`
* **لا تخترع نظام ألوان جديد لأي شاشة جديدة.** استخدم هذه القيم بالضبط.

---

#### 5. بنية الملفات والمسارات — قاعدة صارمة لتفادي مشكلة "sys.path"
* كل الوحدات (`main.py`, `config_manager.py`, `db_connection.py`, `capture_screens.py`) تفترض أن **مجلد العمل الحالي (cwd) عند التشغيل هو** `hr_system/` نفسه.
* **أي ملف يفتح ملفًا آخر بمسار نسبي (`config.ini`, `key.key`, أو أي أصول أخرى مستقبلًا) يجب أن يحسب المسار بالنسبة لموقع الملف البرمجي نفسه، وليس بالنسبة لمجلد التشغيل**، وذلك عبر النمط التالي إلزاميًا:
  ```python
  import os
  BASE_DIR = os.path.dirname(os.path.abspath(__file__))
  config_path = os.path.join(BASE_DIR, "config.ini")
  ```
* هذا النمط **كان مطبّقًا** في نسخة سابقة من `config_manager.py` ثم فُقد أثناء تعديل لاحق. **لا تكرر هذا الخطأ في أي ملف جديد يتعامل مع مسارات نسبية.**
* لا تُضِف أي `sys.path.append(...)` يدوي كحل بديل — المشكلة ليست في استيراد الوحدات (import)، بل في **قراءة/كتابة الملفات** بمسار نسبي لمجلد التشغيل. الحل الصحيح دائمًا هو `BASE_DIR` كما أعلاه، وليس التلاعب بـ `sys.path`.

---

#### 6. قواعد طبقة قاعدة البيانات — إلزامية لأي كود جديد يلمس db_connection.py
1. **كل عملية على قاعدة البيانات تمر عبر** `execute_with_retry` (إعادة محاولة عند قفل السجلات) — لا تفتح اتصال pyodbc مباشرة خارج هذا النمط.
2. **ممنوع أي استعلام غير مُقيَّد** على جدول `var_op` (~1.8 مليون سجل) أو `basic`. كل استعلام يجب أن يحتوي `WHERE` على عمود مفهرس (`EmpId`, `Issue`, `EntryDate`, `ID`) — **لا تكتب أبدًا** `SELECT * FROM var_op` **بدون شرط**.
3. **التجميع (COUNT/GROUP BY) يُنفَّذ داخل SQL**، وليس بجلب كل الصفوف ثم عدّها في Python.
4. **أي استعلام قد يستغرق وقتًا ملحوظًا (بحث بدون رقم موظف محدد، تقرير على مدى واسع) يجب أن يُنفَّذ في** `QThread` **منفصل**، بنفس نمط `NameFetcherThread` المستخدم في شاشة الدخول (خيط + Signal + دالة run)، لتفادي تجميد الواجهة.
5. جدول Access **لا يدعم Server-side Cursor حقيقي عبر pyodbc** — أي `fetchall()` يجلب النتيجة كاملة للذاكرة. لذلك القيد رقم 2 أعلاه ليس اختياريًا بل ضروري للأداء.

---

#### 7. الأمان — لا تُدرج أسرارًا في أي كود أو رسالة
* **لا تكتب كلمة مرور حقيقية أو مفتاح Fernet داخل الكود، التعليقات، أو رسائل الالتزام (commit messages).**
* كلمات المرور تُقرأ دائمًا من `config.ini` المشفّر عبر `config_manager.get_password(db_name)`.
* عند إنشاء بيئة اختبار (كما في `capture_screens.py`)، استخدم مسارات `config.ini`/`key.key` منفصلة ووهمية، لا الملفات الحقيقية.

---

#### 8. بيئة الاختبار (headless)
لا تفترض وجود شاشة عرض فعلية — أي اختبار يجب أن يعمل بدون GUI حقيقي باستخدام `QT_QPA_PLATFORM=offscreen`.

---

#### 9. التوثيق وسجل الحالة الحية — بروتوكول الـ Handover المطور (v16)
* تم إلغاء مجلد `Session_Logs/` وأي ملفات لوجبوك فرعية أو متوازية تماماً.
* المستند الحصري والوحيد المعتمد لمتابعة سياق وحالة وحركة المشروع هو ملف مجمع يقع مباشرة في جذر المشروع (Root) بجانب ملف `main.py` تحت اسم: **`SESSION_HANDOVER_MASTER.md`**
* **بروتوكول إدارة ملف التسليم (Handover Lifecycle)**:
  عند فتح أو إنهاء أي جلسة عمل، يلتزم الوكيل برمجياً بالآتي:
  1. **التحقق من البيئة والمصادر `[ENV_VERIFICATION]`**: فحص لوحة المصادر للتأكد من وجود كافة ملفات الأكواد واللقطات من `1.bmp` إلى `7.bmp`.
  2. **تحديث قسم الموقف النشط `[PART 1: HANDOVER_CURRENT]`**: كتابة تاريخ اليوم والهدف البرمجي اللحظي، مع حظر التراجع البرمجي (Prevent Regression).
  3. **إدراج خريطة الطريق المستهدفة `[NEXT_MILESTONES]`**: توثيق تتابع المهام القادمة بدقة وتحديث حالة المهام المنتهية بـ `DONE ✅`.
  4. **صيانة السجل التاريخي `[PART 2: HANDOVER_ARCHIVE]`**: دمج ملخص المهام المغلقة بانتهاء الجلسة تراكيمًا في نهاية الأرشيف دون مسح أي سطر تاريخي.
  5. **تثبيت القوانين التقنية الصارمة `[HARD_TECHNICAL_RULES]`**: (معالجة أقواس Access المتداخلة، بروتوكول `execute_with_retry` للمحاولات، وعزل المهام الثقيلة في `QThread`).

---

#### 10. نطاق العمل الحالي المؤكَّد والمستقبلي
* **المحطات المغلقة والمؤمنة بالكامل (DONE ✅)**:
  * **المحطة الأولى والثانية**: إصلاح استعلامات Access وحل أخطاء الاتصال.
  * **المحطة الثالثة**: إعادة الهيكلة البصرية لشاشة إدخال المتغيرات وتصميم كروت 2x2.
  * **المحطة الرابعة**: الاختبارات الشاملة وتسليم شاشة المتغيرات بنجاح 100%.
  * **المحطة الخامسة**: إعادة تصميم وتطوير واجهة "تهيئة بداية الشهر" بالكامل وفق معايير Pixel v15 (ألوان تفاعلية أربعة، تلوين ذكي، تثبيت عمود الجمعة أول عمود يميناً، رسالة التحقق التأكيدي الوقائية).
* **المهمة النشطة الحالية (Active Sprint)**:
  * **المحطة السادسة (Station 6)**: برمجة منطق المعالجة الخلفية وعملية التوليد المليونية لجدول `var_op` بالكامل عبر خيط `QThread` مستقل وتحديث شريط التقدم اللحظي.
