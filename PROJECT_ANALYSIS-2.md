# تحليل المشروع الكامل — نظام إدارة الموارد البشرية (HR System)

> **مرجع المطور:** هذا الملف مكتوب للمطور (الذكاء الاصطناعي) في الجلسات القادمة.
> **تاريخ الكتابة:** 18/08/2026
> **مطور المشروع (الإنسان):** "صديق"
> **طريقة الاستخدام:** اقرأ هذا الملف أولاً في أي جلسة جديدة قبل العمل، ثم أكمل من "المهام القادمة".

---

## 1. نظرة عامة

تطبيق **سطح مكتب** لإدارة الموارد البشرية يعمل على شبكة، بلغة **Python + PySide6 + pyodbc**، مع **قاعدتي Microsoft Access**.

- **التصميم:** أسلوب Google Pixel، RTL (من اليمين لليسار)، عربي بالكامل.
- **المستخدم:** صديق — مطور HR برمجيات، يحب الكود النظيف والتصميم المتقن، **لا يحب العمل النص نص**.
- **الموقع على جهاز المستخدم (Windows):** `C:\PROJECTS\amr\external_control_MONKEY_CODE_V3\hr_system\`
- **بيئة التطوير (هنا):** Linux headless — `/workspace/hr_system/` (نسخة المشروع). لا يوجد شاشة GUI؛ للاختبار نستخدم `QT_QPA_PLATFORM=offscreen` مع التقاط صور عبر `capture_screens.py`.

---

## 2. بنية الملفات الحالية

```
hr_system/
├── main.py                     # نقطة الدخول (تسجيل دخول → رئيسية)
├── config.ini                  # الإعدادات (المسارات + كلمات سر مشفرة)
├── config_manager.py           # إدارة الإعدادات + تشفير Fernet لكلمات السر
├── db_connection.py            # الاتصال بـ Access عبر pyodbc + إعادة محاولة
├── key.key                     # مفتاح Fernet لتشفير/فك كلمات السر
├── requirements.txt            # PySide6, pyodbc, cryptography
├── LOGBOOK.md                  # لوجبوك قديم (تاريخي — معلومات الجلسة 1)
├── log.txt                     # لوجبوك قديم نسخة أخرى (تاريخي)
├── capture_screens.py          # سكربت لتقاط صور الشاشات (اختبار headless)
├── setup.py                    # سكريبت إنشاء المشروع (قديم، لا يُستخدم حالياً)
└── ui/
    ├── __init__.py
    ├── login_window.py         # شاشة الدخول
    ├── settings_window.py      # شاشة الإعدادات
    └── main_window.py          # الشاشة الرئيسية
```

---

## 3. التعديلات المنفذة في هذه الجلسة (جلسة 18/08/2026)

### 3.1 شاشة الإعدادات (`ui/settings_window.py`)
- **حذف خياري "اظهار"** لكلمتي السر (كلا الملفين: الرئيسي وTimeSheet).
- **زر الإغلاق**: أصبح اسمه **"خروج"**، أكبر (`minimumHeight=52`, `minimumWidth=140`)، لون فاتح (`#f8f9fa` بخلفية وخلفية hover `#e8eaed`) بدلاً من الأزرق.
- أُزيل استيراد `QCheckBox` غير المستخدم.

### 3.2 شاشة الدخول (`ui/login_window.py`)
- **مربع الاسم** أصبح بنفس لون باقي المربعات (أُزيلت الخلفية الرمادية `#f8f9fa`).
- **إصلاح التأخير/الوميض/ساعة التحميل** عند إدخال الرقم المالي:
  - السبب: استعلام قاعدة البيانات كان يعمل على الخيط الرئيسي.
  - الحل: خيط منفصل `NameFetcherThread` (QThread) + debounce عبر `QTimer` (350ms) + التحقق من تطابق آخر إدخال قبل عرض الاسم.
  - المراجع: `NameFetcherThread` في أعلى الملف، `_name_timer`, `_fetch_name`, `_on_name_fetched`.

### 3.3 الشاشة الرئيسية (`ui/main_window.py`)
- **رسالة الترحيب** في الشريط العلوي: `"مرحبا، {name} | الرقم: {id}"` بدلاً من "المستخدم: ...".
- **الرقم المالي بدون `.0`**: دالة `_format_id()` في السطر ~159 تحول `2107.0` إلى `2107`.
- **أيقونة الإعدادات** أصبحت ترساً حقيقياً عبر `QStyle.StandardPixmap.SP_FileDialogDetailedView` + `QSize(24,24)` بدلاً من علامة الاستفهام.
- أُزيلت رسالة الترحيب الكبيرة من المحتوى.

### 3.4 توحيد ملفات الإعدادات (مهم)
- **المشكلة**: كان هناك `config.ini` و `key.key` داخل `hr_system/` وأخرى في الجذر — ازدواجية سببها المسارات النسبية.
- **الحل**: في `config_manager.py` أصبح `BASE_DIR = os.path.dirname(os.path.abspath(__file__))` والمفتاح/الكونفيج يُقرآن دائماً من `hr_system/`.
- **الدمج**: كلمات السر الكاملة (main + timesheet) مدمجة في `hr_system/config.ini` ومعاد تشفيرها بمفتاح `hr_system/key.key` الصحيح.
- **التنظيف**: حُذف `/workspace/config.ini` و `/workspace/key.key` (نسخة احتياطية في `/tmp/opencode/config_backup/`).
- ⚠️ **أمر مهم للمستخدم**: إذا كانت كلمات السر لا تعمل على جهازه، يجب التأكد أن `key.key` على جهازه مطابق لمفتاح `hr_system/key.key` = `d1J5EV8HRefegNn8lS67DigLRYoArRMKK2yTasWCdN8=`.

### 3.5 مشكلة اتصال شهدناها (لم تُحل نهائياً — بحاجة متابعة)
- المستخدم أبلغ عن: `[Microsoft][ODBC Microsoft Access Driver] Not a valid password (-1905)`.
- التحليل: الخطأ يعني أن Access رفض كلمة السر المرسلة. الاحتمالات:
  1. `key.key` على جهاز المستخدم لا يطابق المفتاح الذي شفّر الكونفيج → فك التشفير يفشل بصمت ويعيد `""`.
  2. كلمة السر الحقيقية لقاعدة البيانات ليست `22092006`.
  3. المسار `C:/PROJECTS/amr/06-08-2026/Source/2025.mdb` قد لا يشير للملف الفعلي.
- فك التشفير في بيئتنا يعطي: `main_pwd → 22092006`, `timesheet_pwd → W@alid_22092006`.
- **تحقق على جهاز المستخدم**: مطابقة محتوى `key.key`, وفتح الـ mdb في Access للتأكد من كلمة السر، وجرب الاتصال بدون كلمة سر.

---

## 4. قاعدة البيانات (تحليل كامل من `all.txt` في `/workspace/all.txt`)

### 4.1 الملف الأول: `2025.mdb` — (يضم `basic` والجداول المساعدة له)

#### `basic` — جدول العاملين (المصدر الأساسي)
| الحقل | النوع | الدلالة |
|-------|-------|---------|
| `ID` | Double | الرقم المالي (مفتاح رئيسي) |
| `name` | Text | الاسم |
| `no3` | Double | النوع |
| `photo` / `PhotoPath` | OLE/Text | الصورة |
| `job` | Long (FK → `jobs.jobs_code`) | الوظيفة |
| `job_group` | Byte | المجموعة النوعية للوظيفة |
| `t2min_no` | Text | الرقم التأميني |
| `birth_date` | Date/Time | تاريخ الميلاد |
| `material_status` | Byte | الحالة الاجتماعية |
| `kawmy_no` | Text | الرقم القومي |
| `adress` / `city` | Text | العنوان / المحافظة |
| `tagnid` | Byte | الموقف من التجنيد |
| `mo2ahel` | Byte | المؤهل الدراسي |
| `level` / `level_2` | Byte | المستوى/الدرجة الوظيفية |
| **`work_place`** | Byte (FK → `work_place.workplacecode`) | **القوة = الموقع الأساسي** |
| `wardya` | Byte | الوردية |
| `sector` / `edara` / `edara_mosa3da` / `edara_3ama` / `eshraf` | Long | الهيكل الإداري |
| `ta3yyn_date` / `keta3_date` / `petrojet` | Date/Time | تواريخ التعيين |
| `fixed_contract` / `open_contract` | Date/Time | العقود |
| **`t3akod`** | Byte (FK → `t3akod.t3akod_code`) | **نوع العقد** |
| `agor` | Byte | توزيع الأجور |
| `enha2_date` | Date/Time | تاريخ إنهاء الخدمة |
| `TopOrEmp` | Byte | عليا أم عاملين |
| `EmpOrdr` | Byte | أجر يومي أم معين |
| `DirectManagerID` / `DeptManagerID` / `GeneralManagerID` | Double | المدراء |
| + حقول أرصدة إجازات (totalras, totalras2, totalras3, totalrasLastYR) | Long | |

#### جداول مساعدة مهمة في 2025:
- `jobs`: `jobs_code` (PK), `jobs`, `job_group`, `level`, `level_2`, `group`, `group_name`
- `work_place`: `workplacecode` (PK), `workplace`, `group1`, `group2`, `group3`, `tsnif`, `wp`, `location`, `remote`, `sea`, `wardya`, `danger`, `dive`, `food`, `transfer`
- `t3akod`: `t3akod_code` (PK), `t3akod`
- `no3`: `CODE` (PK), `NAME`
- `edary`: `agor_ID` (PK), `agor`, `can_do`, `passowrd` — جدول تسجيل الدخول (مصادقة)
- `1_eshraf`, `2_edara_3ama`, `3_edara_mosa3da`, `4_edara`, `5_sector`: هيكل إداري هرمي
- `job_group`, `level`, `level_2`, `level_3`, `material`, `mo2ahel`, `dyana`, `wardya`, `tagnid`, `group`, `group2`, `city`, `code`
- `2023DB`, `2025`, `calc_2022`, `old_basic`, `final2024`: جداول أرصدة/إجازات سنوية تاريخية
- `أجازات` + `أجازات_2023` + `أجازات2018` + `ترصيد الأجازات السنوية`: الإجازات
- `enha2`, `enha2_table`, `enha2_table_old`: إنهاء الخدمة
- `Promotions`: الترقيات
- `FamilyMembers`: أفراد العائلة
- `Relations`, `id`, `group`, `kfaya`, `mwkf_mwaslat`, `مواصلات`, `Dates`

### 4.2 الملف الثاني: `TIME SHEET.mdb` — (يضم جدول التحركات)

#### `var_op` — جدول التحركات (الأساسي للشاشة الجديدة)
| الحقل | النوع | الدلالة |
|-------|-------|---------|
| `Issue` | Text | الشهر بصيغة `YYYYMM` (مثال `202608`) |
| `EmpId` | Double | رقم مالي (مفتاح رئيسي مع التالي) |
| `EntryDate` | Date/Time | التاريخ (مفتاح رئيسي) — صيغة `yyyy/mm/dd` |
| `var` | Byte (FK → `variables.var_code`) | المتغير |
| `wp` | Byte (FK → `work_place.workplacecode`) | الموقع |
| `Notes` | Memo | الأذونات/الملاحظات |
| `lock` | Byte | غير مستخدم حالياً (محتمل استخدامه مستقبلاً للقفل) |

- المفتاح الأساسي: **`EmpId` + `EntryDate`** (فريد) — لا يمكن تكرار نفس اليوم لنفس الموظف.
- فهرس `impID` على `EmpId`.
- حجم ضخم: **~1.8 مليون سجل** → الأداء مهم، استخدم استعلامات مقيّدة بالشهر والفهارس.

#### `variables` — جدول المتغيرات (في TIME SHEET)
| الحقل | النوع |
|-------|-------|
| `var_code` | Byte (PK) |
| `var_name` | Text |
| `var` | Text |

#### `work_place` — في TIME SHEET (نفس اسم الجدول في 2025)
يُستخدم للعلاقة `var_op.wp → work_place.workplacecode`. (انظر جدول `work_place` في 2025 أعلاه).

#### `DatePrep` — جدول أيام الشهر
| الحقل | النوع |
|-------|-------|
| `EDate` | Date/Time |

- **آلية بداية الشهر**: تُرمى أيام الشهر (من اليوم 1 حتى آخر يوم) في `DatePrep`، ثم تُدخل سجلات فارغة لكل عامل عن كل يوم في `var_op`. هذه عملية منفصلة بزر خاص في الشاشة الرئيسية (لاحقاً).

#### جداول أخرى في TIME SHEET:
- `Months`: `MonthCode` (PK), `Month` — الشهور
- `Device`, `finger`, `fingerByDate`: أجهزة البصمة والحضور
- `Holidays`: `Id`, `PDate`, `Var`, `DatCont`, `FromDate`, `ToDate`, `Approve`, `Notes`

### 4.3 العلاقات
```
basic.ID ──┬── var_op.EmpId            (1:M)
variables.var_code ──┬── var_op.var    (1:M)
work_place.workplacecode ──┬── var_op.wp (1:M)
```

---

## 5. شاشة إدخال المتغيرات (شاشة التحركات) — المواصفات الكاملة

> **الحالة:** المواصفات مؤكدة بنسبة كبيرة (جلسة 18/08/2026) — بانتظار صورة التصميم المقترح من المستخدم لتعديل التخطيط النهائي قبل الكود. لا تنفّذ الكود إلا بعد تأكيد المستخدم.

### 5.1 التخطيط العام (مؤقت — ينتظر صورة المستخدم)

```
┌──────────────────────────────┬────────────────────────────────┐
│  (أ) منطقة المدخلات          │  (ب) مخرجات بيان العامل         │
│  - بوكس الشهر + [-][+]       │  - الاسم / الوظيفة / القوة     │
│  - الرقم المالي              │  - نوع التعاقد                 │
│  - تاريخ من / تاريخ إلى      │                                │
│  - متغير (كومبوبوكس)         │                                │
│  - موقع (كومبوبوكس)          │                                │
│  - ملاحظات (تكست بوكس)       │                                │
│  - [ترحيل متغير] [ترحيل إذن] [إلغاء إذن]                       │
├──────────────────────────────┼────────────────────────────────┤
│  (ج) ليست بوكس المتغير فقط   │  (د) [حذف]                    │
│      (جدول variables)        │                                │
│      يمتد لأسفل الشاشة        │                                │
├──────────────────────────────┼────────────────────────────────┤
│  (هـ) تحركات الشهر           │  (و) تقرير سنتين (12-24 صف)    │
│  4 أعمدة: الإذن/الملاحظات،   │  4 أنواع حركة (1-4) + WH(35)   │
│  التاريخ، المتغير، الموقع    │                                │
├──────────────────────────────┴────────────────────────────────┤
│  أزرار: [حذف] [خروج]                                          │
└────────────────────────────────────────────────────────────────┘
```

### 5.2 المدخلات (الجزء أ)
| العنصر | المواصفات |
|--------|-----------|
| بوكس الشهر | نص `YYYYMM`، افتراضي الشهر الحالي، **زران [-][+]** لزيادة/تقليل الشهر بدل كتابة 6 أرقام |
| الرقم المالي | إدخال يدوي. غير موجود في `basic` → رسالة تنبيه + إرجاع المؤشر للبوكس مع **تظليل فقط بدون مسح** |
| تاريخ من | إجباري |
| تاريخ إلى | اختياري، عند الانتقال إليه يملأ تلقائياً بقيمة "من" مع تظليل كامل |
| المتغير | **كومبوبوكس** مع إكمال تلقائي، يقبل إدخال الرقم أو الاسم |
| الموقع | **كومبوبوكس** مع إكمال تلقائي للأسماء |
| ملاحظات | تكست بوكس (مكان إدخال رمز الإذن أو الملاحظة) |
| تنقّل ذكي | الشهر → الرقم المالي → تاريخ من → تاريخ إلى |

### 5.3 مخرجات بيان العامل (الجزء ب) — من `basic`
- الاسم (`name`)، الوظيفة (`job` عبر `jobs`)، القوة (الموقع الأساسي = `work_place` عبر جدول `work_place`)، نوع التعاقد (`t3akod`).
- تُجلب فور إدخال رقم مالي صحيح.

### 5.4 ليست بوكس المتغير (الجزء ج) — عدّل في جلسة اليوم
- **ليست بوكس للمتغير فقط** على يمين الشاشة (مصدره جدول `variables`).
- **مرتبطة بالكومبوبوكس** (ربط ثنائي):
  - اختيار من الليست بوكس → يظهر المتغير في الكومبوبوكس بـ `var_name`.
  - اختيار من الكومبوبوكس → يُحدَّد الصف المقابل في الليست بوكس.
- **الموقع: كومبوبوكس فقط** (لا يوجد له ليست بوكس) — مصدره جدول `work_place` (يعرض `workplace` + `wp`).
- السبب: سهولة الاستخدام للمستخدم المتمرس (ليست) والمستخدم الجديد (كومبوبوكس).

### 5.5 تحركات الشهر (الجزء هـ)
- جدول يعرض سجلات الشهر الحالي للعامل في 4 أعمدة:
  **الإذن/الملاحظات** | **تاريخ اليوم** | **المتغير** | **الموقع** — من `var_op`.
- **عند الوقوف على صف/يوم**: تُنقل بيانات الأربع حقول للجانب الأيسر:
  - اليوم → خانة "تاريخ من" **و"تاريخ إلى" معاً** (مؤكد من المستخدم — 18/08/2026).
  - المتغير → يظهر في الليست بوكس والكومبوبوكس المرتبط.
  - الموقع → يظهر في كومبوبوكس الموقع.
  - الإذن/الملاحظات → تظهر في تكست بوكس الملاحظات.

### 5.6 تقرير العام الحالي والسابق (الجزء و) — عدّل في جلسة اليوم
- **متغير خامس جديد**: `WH` بكود `35` يُضاف للجدول.
- التقرير عن **العام الحالي والسابق** (وليس 6 شهور كما كان مقترحاً): حد أدنى **12 صفاً** وحد أقصى **24 صفاً**.
- **شكل العرض**: ليست بوكس يظهر **12 صفاً** والسكرول بار ينقل للباقي.
- المحتوى: عدد أيام لكل نوع من الأنواع (1, 2, 3, 4 + WH/35) + الإجمالي.

### 5.7 قواعد العمل — مؤكدة
- **الأذونات الشهرية**: كل نوع له **مرتان كحد أقصى شهرياً** = إجمالاً `2` مرتين لرمز `2` + مرتين لرمز `x` (4 أذونات كحد أقصى شهرياً). التحقق على مستوى **الشهر كله** (ليس لكل نطاق).
  - كتابة التاريخ + رمز الإذن في الملاحظات → يرحّل الإذن لعمود الأذونات.
  - تجاوز المرتين لأي رمز → رسالة رفض + إلغاء العملية.
- **نطاق التواريخ (من → إلى)**: يُطبّق على **كل الأيام في النطاق** (مثال: من 1 إلى 5 = 5 أيام بنفس القيمة).
- **تحديث السجلات**:
  - إن وُجد السجل (مُعبّأ مسبقاً في بداية الشهر) → **تحديث** القيمة الموجودة.
  - إن لم يوجد السجل → **رسالة تنبيه**: "لا يوجد سجل — يجب إدراج شهر جديد في جدول الحضور".
- **الحذف**: زر حذف لحذف حركة من عمود المتغيرات أو إذن من عمود الملاحظات؛ وإن وُجد الاثنان في نفس اليوم يُخيَّر المستخدم.
- `lock` يُترك دون استخدام.

### 5.8 الأزرار — مؤكدة (تم تعديلها في جلسة اليوم)
- **[ترحيل متغير]**: (كان "تنفيذ متغير") — يرحّل حركة اليوم (تاريخ – متغير – موقع).
- **[ترحيل إذن]**: (كان "تنفيذ إذن") — يرحّل الإذن من بوكس الملاحظات إلى عمود الأذونات/الملاحظات.
- **[إلغاء إذن]**: يلغي الإذن من خانة الملاحظات في التاريخ المحدد. إذا لم يوجد تاريخ → رسالة "برجاء اختيار التاريخ".
- **[حذف]**: حذف حركة/إذن (مع التخيير إن وُجد الاثنان في نفس اليوم).
- **[خروج]**: إغلاق الشاشة.
- **⚠️ زر "ترحيل" الكبير المدمج أُلغي** — لا يوجد زر ترحيل واحد؛ كل عملية بزره الخاص.
- مع قيمة فارغة عند الضغط على زر الترحيل → **تفريغ الخلية** في النطاق كله مع رسالة تنبيه.
- 📌 **الحالة (19/08/2026)**: المواصفات مؤكدة + صورة الكروكي وصلت (`/workspace/كروكي شاشة الادخال.bmp`). **بانتظار كلمة "اشتغل" من المستخدم لبدء الكود.**

### 5.10 حل مشكلة ظهور الجداول على الشاشات الصغيرة (مؤكد 19/08/2026)
- **المشكلة**: الجداول الثابتة الارتفاع تُظهر ~80% فقط على الشاشات الصغيرة (يحتاج تمرير مزعج).
- **البيانات محدودة**: متغيرات = 37، شهر = 31 يوم كحد أقصى، تقرير = 24 صف كحد أقصى.
- **الحل المختار (مبسّط، بدون تعقيد، بلا تأثير على الأداء)**:
  1. **نسب مرنة (Stretch)**: توزيع الجداول بنسب (المدخلات ~20% / الجداول ~50% / التقرير ~30%) لتمدد الجداول حسب الشاشة المتاحة.
  2. **نافذة Maximized**: تفتح الشاشة بأقصى حجم عبر `showMaximized()`.
  3. **DPI Scaling**: تفعيل `Qt.AA_EnableHighDpiScaling` في `main.py` + أبعاد نسبية (لا ثابتة).
  4. السكرول الداخلي للجدول (مدمج في Qt) يظهر فقط في الحالات الاستثنائية — لا QSplitter (تعقيد غير مبرر مع بيانات ثابتة).
- **البيانات الثابتة (قيم مؤكدة)**: جدول المتغيرات = 37 متغيراً، جدول الشهر = 31 يوماً كحد أقصى، والتقرير = 24 صفاً كحد أقصى.

---

## 6. شاشات النظام الحالية (سريان)

### 6.1 شاشة الدخول (`ui/login_window.py`)
- 3 حقول: الرقم المالي + الاسم (تلقائي) + كلمة المرور.
- دخول وخروج. مصادقة من جدول `edary` في 2025.mdb عبر `db.authenticate`:
  ```sql
  SELECT e.ID, e.agor, e.IsDeveloper, b.name
  FROM edary e LEFT JOIN basic b ON e.ID = b.ID
  WHERE e.ID = ? AND e.passowrd = ?
  ```
- ⚠️ **ملاحظة**: الاستعلام يستخدم `edary.IsDeveloper` و `edary.passowrd`، لكن جدول `edary` في `all.txt` يظهر بحقول `agor_ID, agor, can_do, passowrd` فقط (لا يوجد `IsDeveloper`).
- ✅ **المؤكد من المستخدم (جلسة 18/08/2026)**: حقل `IsDeveloper` **موجود فعلاً** في جدول `edary` الحقيقي، ويوجد **مستخدم واحد فقط** حالياً يملك هذه الصلاحية.

### 6.2 شاشة الإعدادات (`ui/settings_window.py`)
- مساران للملفات (2025.mdb و TimeSheet.mdb) + كلمة سر لكل ملف.
- فحص تلقائي: الاسم يجب أن يبدأ بـ `2025` أو `TimeSheet` وينتهي بـ `.mdb`/`.accdb`.
- حفظ تلقائي عند النجاح. زر خروج.

### 6.3 الشاشة الرئيسية (`ui/main_window.py`)
- شريط علوي: ترحيب + رقم + زر الترس (الإعدادات).
- زر واحد حالياً: "ادخال المتغيرات" → يفتح `QMessageBox "قريبا"` (تحت التطوير) — **هذا ما سيربط بشاشة التحركات**.

---

## 7. الاتصال بقاعدة البيانات (`db_connection.py`)

- الاتصال: `pyodbc` مع `DRIVER={Microsoft Access Driver (*.mdb, *.accdb)}` و `DBQ=<path>` و `PWD=<password>` اختيارياً.
- `execute_with_retry`: إعادة محاولة عند قفل السجلات (`retry_attempts` من config, `retry_delay_ms`).
- الدوال: `connect`, `execute_with_retry`, `test_connection`, `authenticate`, `get_employee_name`.
- ملاحظة: كلمة السر تُمرر عبر الكونفيج المشفر. **تحقق من مشكلة كلمة السر المذكورة في 3.5**.

---

## 8. بيئة التطوير والاختبار (Linux headless)

- تثبيت المتطلبات: `pip3 install --break-system-packages PySide6 pyodbc cryptography`
- مكتبات النظام اللازمة (تم تثبيتها): `libgl1 libglib2.0-0 libegl1 libxkbcommon0 libxkbcommon-x11-0 libxcb-cursor0 libxcb-icccm4 libxcb-keysyms1 libxcb-shape0 libxcb-xinerama0 libfontconfig1 libfreetype6 fontconfig libdbus-1-3 unixodbc` + خطوط `fonts-noto-core fonts-noto-ui-core`.
- **تشغيل GUI بدون شاشة**: `QT_QPA_PLATFORM=offscreen python3 main.py`
- **التقاط صور للشاشات**: `QT_QPA_PLATFORM=offscreen python3 capture_screens.py` → يخزن PNG في `/tmp/opencode/screens/` ثم عرضها عبر خادم HTTP + `mcaiBuiltin_request_preview`.
- **تشغيل سريع للتحقق من بناء واجهة**: مثال في `capture_screens.py`.
- ⚠️ لا يوجد `python` (فقط `python3`). `main.py` يحتاج PySide6 (مثبت في البيئة الحالية).

---

## 9. معلومات المستخدم وأسلوب العمل

- المستخدم اسمه **صديق**، يعمل على نظام **Windows** ويشغّل المشروع محلياً من `C:\PROJECTS\amr\external_control_MONKEY_CODE_V3\hr_system\`.
- يفضل: الرفع عبر ملفات مضغوطة، أو تصحيح مباشر هنا ثم إعادة التحزيل.
- **ملاحظة مهمة**: لا يوجد دمج Git بين بيئتنا وجهازه. يُنسق العمل عبر ملفات.
- **قاعدة**: لا تنفّذ كوداً إلا بعد أن يقول المستخدم "اشتغل". النقاش أولاً.
- **التصميم Pixel Style RTL** هو المعيار.

---

## 10. المهام القادمة (ترتيب مقترح)
1. **شاشة إدخال المتغيرات (التحركات)** — المواصفات مؤكدة في القسم 5. بانتظار **صورة التصميم المقترح** من المستخدم (ستُرفع في الوركسبيس) لتعديل التخطيط النهائي، ثم الكود.
2. زر تهيئة بداية الشهر (DatePrep + تعبئة السجلات الفارغة) — في الشاشة الرئيسية.
3. شاشة البحث برقم العامل.
4. تقارير.
5. حل نهائي لمشكلة كلمة السر/الاتصال (3.5) — تحقق مع المستخدم على جهازه.
6. ملاحظة: `edary.IsDeveloper` مؤكد وجوده — لا حاجة للمزيد من التحقيق.

---

## 11. ملفات مرجعية أخرى في /workspace
- `/workspace/all.txt` — تحليل كامل لقواعد البيانات (932 سطر): كل الجداول والحقول والعلاقات والملفاتين.
- `/workspace/hr_system/LOGBOOK.md` و `log.txt` — لوجبوك قديم (جلسة 1) لسياق تاريخي.
- `/workspace/hr_system/capture_screens.py` — سكربت التقاط الشاشات للاختبار.
